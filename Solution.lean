import LeftPCI

/-!
# Proved solution

Comparator checks that `LeftPCI.exists_isLeftPCIRing_not_isRightPCIRing`,
`LeftPCI.not_forall_isLeftPCIRing_imp_isRightPCIRing` and
`LeftPCI.not_forall_isLeftVRing_imp_isRightVRing` have exactly the same statements here as in
`Challenge.lean` (with the definitions of `LeftPCI.Counterexample.Defs`, which are literally those
of `Challenge.lean`) and that their proofs use only the permitted axioms. The theorems are proved in
`LeftPCI.Counterexample.Closure`; this module adds no declarations of its own.
-/
